#!/usr/bin/env bash
set -euo pipefail

GEM_PLATFORM=x86_64-linux rake build
GEM_PLATFORM=arm64-linux rake build
GEM_PLATFORM=aarch64-linux rake build

GEM_PLATFORM=x86_64-darwin rake build
GEM_PLATFORM=arm64-darwin rake build

GEM_PLATFORM=x64-mingw-ucrt rake build
GEM_PLATFORM=arm64-mingw-ucrt rake build
